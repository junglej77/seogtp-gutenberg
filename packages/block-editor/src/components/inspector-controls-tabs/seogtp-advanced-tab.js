/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';

/**
 * Internal dependencies
 */
import InspectorControls from '../inspector-controls';

const AdvancedTab = ( { blockName, clientId } ) => {
	return (
		<>
			高级设置
		</>
	);
};

export default AdvancedTab;
